

/**
 * Service equivalent of workflow.writeControlResults( ts, controlId)
 * 
 */

writeControlResults = EventService {
  reference: control
	selection: writeControlResults
	timeslot =  Input { type: String }		
	controlIds =  Input { type: String multivalued: True mandatory: True }
	error = Output 
}

