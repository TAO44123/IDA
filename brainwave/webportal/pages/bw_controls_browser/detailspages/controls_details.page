/* Import other pages here */
import "/webportal/pages/bw_controls_browser/detailspages/templates/control_on_application.page"
import "/webportal/pages/bw_controls_browser/detailspages/templates/control_on_permission.page"
import "/webportal/pages/bw_controls_browser/detailspages/templates/control_on_accounts.page"
import "/webportal/pages/bw_controls_browser/detailspages/templates/control_on_identities.page"
import "/webportal/pages/bw_controls_browser/detailspages/templates/control_on_assets.page"
import "/webportal/pages/bw_controls_browser/detailspages/templates/control_on_groups.page"
import "/webportal/pages/bw_controls_browser/detailspages/templates/control_on_organisations.page"
import "/webportal/pages/bw_controls_browser/detailspages/templates/control_on_accountsWithRights.page"
import "/webportal/pages/bw_controls_browser/resources/NLS.page"

/**
 * <span style="white-space: pre-line">
 * The dialog designed to call the control on app detail fragment
 * 
 * <u>Parameters:</u><ul>
 * <li><b>controlType</b> (in) the designed control type</li>
 * <li><b>controlCode</b> (in) the designed control code</li>
 * </ul>
 * </span>
 */
bw_control_browser_controls_details_on_app = Dialog {
	title: controlName
	width: 950
	height: 650
	
	buttons:
		[
			ActionButton {
				text: $bw_control_browser.close
				close: True
				actions:
					Unset dummy
			}
		]
	
	// Dummy variable for ActionButton
	dummy = Variable { type: String }
	// Control information
	controlCode = Variable  { type: String }
	controlType = Variable { type: String }
	controlName = Variable { type: String }
	updateRequired = Variable { type: Boolean }
	useExceptions = Variable { type: Boolean }
	// Type of entity (Account, Perm, ... or Right, Share, etc.)
	type = Variable { type: String }
	// KPI information
	selectedDiscrepancies = Variable { type: Integer }
	selectedException = Variable { type: Integer }
	selectedDiscrepanciesTrend = Variable { type: Integer }
	selectedExceptionTrend = Variable { type: Integer }
	
	IncludeFragment {
		fragment: bw_control_browser_control_on_app with
			value-of controlCode to controlCode,
			value-of controlType to controlType,
			value-of useExceptions to useExceptions,
			value-of type to type,
			updateRequired binds-to updateRequired,
			value-of selectedDiscrepancies to selectedDiscrepancies,
			value-of selectedDiscrepanciesTrend to selectedDiscrepanciesTrend,
			value-of selectedException to selectedException,
			value-of selectedExceptionTrend to selectedExceptionTrend
	}
}

/**
 * <span style="white-space: pre-line">
 * The dialog designed to call the control on acc detail fragment
 * 
 * <u>Parameters:</u><ul>
 * <li><b>controlType</b> (in) the designed control type</li>
 * <li><b>controlCode</b> (in) the designed control code</li>
 * </ul>
 * </span>
 */
bw_control_browser_controls_details_on_acc = Dialog {
	title: controlName
	width: 1024
	height: 768
	
	buttons:
		[
			ActionButton {
				text: $bw_control_browser.close
				close: True
				actions:
					Unset dummy
			}
		]
	
	// Dummy variable for ActionButton
	dummy = Variable { type: String }
	// Control information
	controlCode = Variable { type: String }
	controlType = Variable { type: String }
	controlName = Variable { type: String }
	updateRequired = Variable { type: Boolean }
	useExceptions = Variable { type: Boolean }
	// KPI information
	selectedDiscrepancies = Variable { type: Integer }
	selectedException = Variable { type: Integer }
	selectedDiscrepanciesTrend = Variable { type: Integer }
	selectedExceptionTrend = Variable { type: Integer }
	
	IncludeFragment {
		fragment: bw_control_browser_control_on_accounts with
			value-of controlCode to controlCode,
			value-of controlType to controlType,
			value-of useExceptions to useExceptions,
			updateRequired binds-to updateRequired,
			value-of selectedDiscrepancies to selectedDiscrepancies,
			value-of selectedDiscrepanciesTrend to selectedDiscrepanciesTrend,
			value-of selectedException to selectedException,
			value-of selectedExceptionTrend to selectedExceptionTrend
	}
}

/**
 * <span style="white-space: pre-line">
 * The dialog designed to call the control on acc + rights detail fragment
 * 
 * <u>Parameters:</u><ul>
 * <li><b>controlType</b> (in) the designed control type</li>
 * <li><b>controlCode</b> (in) the designed control code</li>
 * </ul>
 * </span>
 */
bw_control_browser_controls_details_on_acc_rights = Dialog {
	title: controlName
	width: 1200
	height: 800
	
	buttons:
		[
			ActionButton {
				text: $bw_control_browser.close
				close: True
				actions:
					Unset dummy
			}
		]
	
	// Dummy variable for ActionButton
	dummy = Variable { type: String }
	// Control information
	controlCode = Variable { type: String }
	controlType = Variable { type: String }
	controlName = Variable { type: String }
	updateRequired = Variable { type: Boolean }
	useExceptions = Variable { type: Boolean }
	// KPI information
	selectedDiscrepancies = Variable { type: Integer }
	selectedException = Variable { type: Integer }
	selectedDiscrepanciesTrend = Variable { type: Integer }
	selectedExceptionTrend = Variable { type: Integer }
	
	IncludeFragment {
		fragment: bw_control_browser_control_on_accountsWithRights with
			value-of controlCode to controlCode,
			value-of controlType to controlType,
			value-of useExceptions to useExceptions,
			updateRequired binds-to updateRequired,
			value-of selectedDiscrepancies to selectedDiscrepancies,
			value-of selectedDiscrepanciesTrend to selectedDiscrepanciesTrend,
			value-of selectedException to selectedException,
			value-of selectedExceptionTrend to selectedExceptionTrend
	}
}

/**
 * <span style="white-space: pre-line">
 * The dialog designed to call the control on perm detail fragment
 * 
 * <u>Parameters:</u><ul>
 * <li><b>controlType</b> (in) the designed control type</li>
 * <li><b>controlCode</b> (in) the designed control code</li>
 * </ul>
 * </span>
 */
bw_control_browser_controls_details_on_perm = Dialog {
	title: controlName
	width: 950
	height: 650
	
	buttons:
		[
			ActionButton {
				text: $bw_control_browser.close
				close: True
				actions:
					Unset dummy
			}
		]
	
	// Dummy variable for ActionButton
	dummy = Variable { type: String }
	// Control information
	controlCode = Variable { type: String }
	controlType = Variable { type: String }
	controlName = Variable { type: String }
	updateRequired = Variable { type: Boolean }
	useExceptions = Variable { type: Boolean }
	// Type of entity (Account, Perm, ... or Right, Share, etc.)
	type = Variable { type: String }
	// KPI information
	selectedDiscrepancies = Variable { type: Integer }
	selectedException = Variable { type: Integer }
	selectedDiscrepanciesTrend = Variable { type: Integer }
	selectedExceptionTrend = Variable { type: Integer }

	IncludeFragment {
		fragment: bw_control_browser_control_on_permission with
			value-of controlCode to controlCode,
			value-of controlType to controlType,
			value-of useExceptions to useExceptions,
			value-of type to type,
			updateRequired binds-to updateRequired,
			value-of selectedDiscrepancies to selectedDiscrepancies,
			value-of selectedDiscrepanciesTrend to selectedDiscrepanciesTrend,
			value-of selectedException to selectedException,
			value-of selectedExceptionTrend to selectedExceptionTrend
	}
}

/**
 * <span style="white-space: pre-line">
 * The dialog designed to call the control on identities detail fragment
 * 
 * <u>Parameters:</u><ul>
 * <li><b>controlType</b> (in) the designed control type</li>
 * <li><b>controlCode</b> (in) the designed control code</li>
 * </ul>
 * </span>
 */
bw_control_browser_controls_details_on_identities = Dialog {
	title: controlName
	width: 950
	height: 650
	
	buttons:
		[
			ActionButton {
				text: $bw_control_browser.close
				close: True
				actions:
					Unset dummy
			}
		]
	
	// Dummy variable for ActionButton
	dummy = Variable { type: String }
	// Control information
	controlCode = Variable { type: String }
	controlType = Variable { type: String }
	controlName = Variable { type: String }
	updateRequired = Variable { type: Boolean }
	useExceptions = Variable { type: Boolean }
	// KPI information
	selectedDiscrepancies = Variable { type: Integer }
	selectedException = Variable { type: Integer }
	selectedDiscrepanciesTrend = Variable { type: Integer }
	selectedExceptionTrend = Variable { type: Integer }

	IncludeFragment {
		fragment: bw_control_browser_control_on_identities with
			value-of controlCode to controlCode,
			value-of controlType to controlType,
			value-of useExceptions to useExceptions,
			updateRequired binds-to updateRequired,
			value-of selectedDiscrepancies to selectedDiscrepancies,
			value-of selectedDiscrepanciesTrend to selectedDiscrepanciesTrend,
			value-of selectedException to selectedException,
			value-of selectedExceptionTrend to selectedExceptionTrend
	}
}

/**
 * <span style="white-space: pre-line">
 * The dialog designed to call the control on groups detail fragment
 * 
 * <u>Parameters:</u><ul>
 * <li><b>controlType</b> (in) the designed control type</li>
 * <li><b>controlCode</b> (in) the designed control code</li>
 * </ul>
 * </span>
 */
bw_control_browser_controls_details_on_groups = Dialog {
	title: controlName
	width: 950
	height: 650
	
	buttons:
		[
			ActionButton {
				text: $bw_control_browser.close
				close: True
				actions:
					Unset dummy
			}
		]
	
	// Dummy variable for ActionButton
	dummy = Variable { type: String }
	// Control information
	controlCode = Variable { type: String }
	controlType = Variable { type: String }
	controlName = Variable { type: String }
	updateRequired = Variable { type: Boolean }
	useExceptions = Variable { type: Boolean }
	// KPI information
	selectedDiscrepancies = Variable { type: Integer }
	selectedException = Variable { type: Integer }
	selectedDiscrepanciesTrend = Variable { type: Integer }
	selectedExceptionTrend = Variable { type: Integer }

	IncludeFragment {
		fragment: bw_control_browser_control_on_groups with
			value-of controlCode to controlCode,
			value-of controlType to controlType,
			value-of useExceptions to useExceptions,
			updateRequired binds-to updateRequired,
			value-of selectedDiscrepancies to selectedDiscrepancies,
			value-of selectedDiscrepanciesTrend to selectedDiscrepanciesTrend,
			value-of selectedException to selectedException,
			value-of selectedExceptionTrend to selectedExceptionTrend
	}
}

/**
 * <span style="white-space: pre-line">
 * The dialog designed to call the control on assets detail fragment
 * 
 * <u>Parameters:</u><ul>
 * <li><b>controlType</b> (in) the designed control type</li>
 * <li><b>controlCode</b> (in) the designed control code</li>
 * </ul>
 * </span>
 */
bw_control_browser_controls_details_on_assets = Dialog {
	title: controlName
	width: 950
	height: 650
	
	buttons:
		[
			ActionButton {
				text: $bw_control_browser.close
				close: True
				actions:
					Unset dummy
			}
		]
	
	// Dummy variable for ActionButton
	dummy = Variable { type: String }
	// Control information
	controlCode = Variable { type: String }
	controlType = Variable { type: String }
	controlName = Variable { type: String }
	updateRequired = Variable { type: Boolean }
	useExceptions = Variable { type: Boolean }
	// KPI information
	selectedDiscrepancies = Variable { type: Integer }
	selectedException = Variable { type: Integer }
	selectedDiscrepanciesTrend = Variable { type: Integer }
	selectedExceptionTrend = Variable { type: Integer }

	IncludeFragment {
		fragment: bw_control_browser_control_on_assets with
			value-of controlCode to controlCode,
			value-of controlType to controlType,
			value-of useExceptions to useExceptions,
			updateRequired binds-to updateRequired,
			value-of selectedDiscrepancies to selectedDiscrepancies,
			value-of selectedDiscrepanciesTrend to selectedDiscrepanciesTrend,
			value-of selectedException to selectedException,
			value-of selectedExceptionTrend to selectedExceptionTrend
	}
}

/**
 * <span style="white-space: pre-line">
 * The dialog designed to call the control on assets detail fragment
 * 
 * <u>Parameters:</u><ul>
 * <li><b>controlType</b> (in) the designed control type</li>
 * <li><b>controlCode</b> (in) the designed control code</li>
 * </ul>
 * </span>
 */
bw_control_browser_controls_details_on_organisations = Dialog {
	title: controlName
	width: 950
	height: 650
	
	buttons:
		[
			ActionButton {
				text: $bw_control_browser.close
				close: True
				actions:
					Unset dummy
			}
		]
	
	// Dummy variable for ActionButton
	dummy = Variable { type: String }
	// Control information
	controlCode = Variable { type: String }
	controlType = Variable { type: String }
	controlName = Variable { type: String }
	updateRequired = Variable { type: Boolean }
	useExceptions = Variable { type: Boolean }
	// KPI information
	selectedDiscrepancies = Variable { type: Integer }
	selectedException = Variable { type: Integer }
	selectedDiscrepanciesTrend = Variable { type: Integer }
	selectedExceptionTrend = Variable { type: Integer }

	IncludeFragment {
		fragment: bw_control_browser_control_on_organisations with
			value-of controlCode to controlCode,
			value-of controlType to controlType,
			value-of useExceptions to useExceptions,
			updateRequired binds-to updateRequired,
			value-of selectedDiscrepancies to selectedDiscrepancies,
			value-of selectedDiscrepanciesTrend to selectedDiscrepanciesTrend,
			value-of selectedException to selectedException,
			value-of selectedExceptionTrend to selectedExceptionTrend
	}
}
